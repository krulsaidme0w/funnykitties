#include "Tsqueue.h"

namespace network {

        const T& Tsqueue::front() {
        }

        const T& Tsqueue::back() {
        }

        T Tsqueue::pop_front() {
        }

        T Tsqueue::pop_back() {
        }

        void Tsqueue::push_back(const T& item) {
        }

        void Tsqueue::push_front(const T& item) {
        }

        bool Tsqueue::empty() {
        }

        size_t Tsqueue::count() {
        }

        void Tsqueue::clear() {
        }

        void Tsqueue::wait() {
        }
    }
} // namespace network
